class User_task_project {
  constructor(id, userId, taskId, projectId) {
    this.id = id;
    this.userId = userId;
    this.taskId = taskId;
    this.projectId = projectId;
  }
}

module.exports = User_task_project;